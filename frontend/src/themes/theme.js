import {createTheme} from '@material-ui/core/styles';

const theme = createTheme({
    palette: {
        background: {
          default:  '#DEE4E7',
        },
        primary: {
            main: '#736a4c',
        },
        secondary: {
            main: '#003366',
        },
        navbar: {
            main: '#e7e7e7',
        }
    },
});

export default theme;
